---
aliases:
  - ege
  - exam
  - егэ
  - экзамен
tags:
  - ege
  - exam
date: 2025-12-22
---
# Описание
В этом типе заданий представлена [[реляционная база данных]], состоящая из нескольких связанных таблиц. Каждая таблица содержит информацию о конкретных объектах: например, о товарах, клиентах, операциях, местоположениях, событиях и т.п. — в зависимости от контекста задачи.

📌 **Ключевая особенность**: таблицы **не являются независимыми** — они связаны между собой через общие поля (например, `ID`, `Артикул`, `Дата`, `Номер заказа` и т.д.). Это позволяет объединять данные из разных таблиц для получения итоговой информации.

Цель — на основе заданного условия (например, «сумма продаж по определённому отделу в определённых магазинах за период») выполнить **многотабличный анализ**:

- отфильтровать нужные строки по критериям (дата, тип операции, адрес, отдел и т.д.);
- объединить данные из нескольких таблиц по связующим полям;
- вычислить итоговое значение (сумму, количество, среднее и т.п.);
- записать ответ в требуемом формате.

---
# Принцип решения
## Общая логика подхода

Такие задачи требуют анализа данных, распределённых по нескольким таблицам, связанным между собой через общие поля. Цель — объединить данные, отфильтровать нужные строки и вычислить итоговое значение, указанное в условии.

Решение строится на **последовательном объединении таблиц** с использованием функций Excel, в первую очередь — `ВПР` (или `XВПР` / `ПОИСКПОЗ` + `ИНДЕКС`), и **логической фильтрации** с последующим агрегированием.

---
## Пошаговый план решения

### 1. Определить «ведущую» таблицу

- Это таблица, содержащая основные операции или события, к которым нужно подтянуть данные из других таблиц.
- Обычно это таблица с наибольшим количеством строк или с ключевыми действиями (например, «Движение товаров», «Продажи», «Заказы»).
- Именно к ней будут присоединяться данные из других таблиц.

> Пример: если нужно посчитать выручку по продажам — ведущей будет таблица с операциями «Продажа».

---

### 2. Определить, какие данные нужно подтянуть из других таблиц

- Из условия задачи выделить критерии фильтрации (например: отдел = «Конфеты», адрес содержит «Металлургов», дата от 7 до 14 июня).
- Определить, из каких таблиц берутся эти данные:
  - Отдел → таблица «Товар»
  - Адрес → таблица «Магазин»
  - Цена за упаковку → таблица «Товар»

---

### 3. Подключить данные из других таблиц с помощью ВПР

- В ведущей таблице добавить новые столбцы для нужных данных (например, «Отдел», «Адрес», «Цена за упаковку»).
- Использовать функцию ВПР (или XВПР), чтобы подтянуть данные из других таблиц по ключевому полю (например, «Артикул», «ID магазина»).

> Важно: фиксировать диапазон поиска с помощью $ — например:  
> =ВПР(A2; \$D\$2:\$G\$100; 3; ЛОЖЬ) — чтобы при копировании формулы диапазон не смещался.

---

### 4. Отфильтровать нужные строки

- Использовать автофильтр в Excel — стандартный инструмент, доступный через стрелку в заголовке столбца.
- Применять фильтры по каждому нужному столбцу
- Можно также добавить вспомогательный столбец с логическим условием, если нужно объединить несколько условий в одном фильтре.

---

### 5. Вычислить итоговое значение

- Умножить количество упаковок на цену за упаковку — чтобы получить выручку по каждой строке.
- Использовать СУММ или СУММЕСЛИМН, чтобы посчитать итог по отфильтрованным строкам.
- Если требуется целая часть — использовать ЦЕЛОЕ() или просто отбросить дробную часть при записи ответа.

---

### 6. Записать ответ

- В ответе указать целое число (если требуется), без дополнительных символов.
- Проверить, что все условия учтены: даты, тип операции, отдел, адрес, и т.д.

---

## Справочная информация: самые распространённые функции Excel

| Функция    | Назначение                                                                         | Пример использования                                                      |
| ---------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| ВПР        | Ищет значение в первом столбце таблицы и возвращает значение из указанного столбца | =ВПР(A2; \$D\$2:\$G\$100; 3; ЛОЖЬ)                                        |
| XВПР       | Современная версия ВПР, более гибкая и безопасная                                  | =XВПР(A2; D:G; 3; ЛОЖЬ)                                                   |
| СУММ       | Суммирует числа в диапазоне                                                        | =СУММ(A2:A100)                                                            |
| СУММЕСЛИМН | Суммирует значения по нескольким условиям                                          | =СУММЕСЛИМН(C:C; A:A; ">=7.06.2024"; A:A; "<=14.06.2024"; B:B; "Продажа") |
| СЧЁТЕСЛИМН | Считает количество строк по нескольким условиям                                    | =СЧЁТЕСЛИМН(A:A; ">=7.06.2024"; B:B; "Продажа")                           |
| ЕСЛИ       | Возвращает одно значение, если условие истинно, другое — если ложно                | =ЕСЛИ(A2>10; "Да"; "Нет")                                                 |
| И, ИЛИ     | Проверяют несколько условий одновременно                                           | =И(A2>=7.06.2024; A2<=14.06.2024; B2="Продажа")                           |
| ПОИСК      | Ищет подстроку в тексте                                                            | =ПОИСК("Металлургов"; A2)                                                 |
| ЕОШИБКА    | Проверяет, является ли значение ошибкой                                            | =ЕСЛИ(ЕОШИБКА(ПОИСК("Металлургов"; A2)); ЛОЖЬ; ИСТИНА)                    |
| ЦЕЛОЕ      | Округляет число вниз до целого                                                     | =ЦЕЛОЕ(123.78) → 123                                                      |
| ФИЛЬТР     | Возвращает строки, удовлетворяющие условиям (Excel 365)                            | =ФИЛЬТР(A:C; (A:A>=7.06.2024)*(A:A<=14.06.2024)*(B:B="Продажа"))          |

---
# Примеры

---
