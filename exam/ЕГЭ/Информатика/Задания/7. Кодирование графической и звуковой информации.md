---
aliases:
  - ege
  - exam
  - егэ
  - экзамен
tags:
  - ege
  - exam
date: 2025-12-24
---
# Описание
## Кодирование и передача мультимедийных данных

В этом типе задач рассматривается **обработка, хранение и передача мультимедийных данных** — изображений, аудио, видео — с учётом их технических характеристик:

- **Разрешение** (количество пикселей по горизонтали и вертикали).
- **Глубина цвета** (количество бит на пиксель или количество цветов в палитре).
- **Частота дискретизации** (для аудио и видео — сколько раз в секунду снимается кадр или отсчёт).
- **Количество каналов** (моно, стерео, 5.1 и т.д.).
- **Скорость передачи данных** (бит/с, Кбит/с, Мбит/с).
- **Сжатие** (коэффициент сжатия, процент сжатия, алгоритм сжатия).
- **Объём памяти** (в байтах, Кбайт, Мбайт, Гбайт).
- **Время передачи** (в секундах, минутах, часах).

Цель — **рассчитать один из параметров**, зная остальные. 
## Типы задач (по содержанию)

1. **Расчёт объёма файла** — зная разрешение, глубину цвета, длительность, частоту, количество каналов.
2. **Расчёт времени передачи** — зная объём файла и скорость канала.
3. **Расчёт минимального объёма памяти** — зная количество файлов и их размер.
4. **Расчёт максимального количества файлов** — зная объём памяти и размер одного файла.
5. **Расчёт степени сжатия** — зная исходный и сжатый объём.
6. **Расчёт скорости передачи** — зная объём и время.
7. **Расчёт частоты дискретизации или разрешения** — зная объём, время, количество каналов и т.д.
8. И т. д.

---
# Теория
## 📚 Теоретическая основа: ключевые понятия и формулы

Прежде чем решать задачи, важно понимать, **как рассчитывается объём данных** для разных типов мультимедиа — изображений, аудио, видео. Все формулы строятся на одном принципе:

> **Объём данных = количество элементов × количество бит на элемент**

### 1. 🖼️ Для изображений (фотографий)

**Формула объёма (в битах):**  
$$
V = W \times H \times i
$$

- $W$ — ширина (в пикселях)  
- $H$ — высота (в пикселях)  
- $i$ — глубина цвета (бит на пиксель)

 Если дано количество цветов $N$, то:  
 $$
 i = \log_2 N
 $$  
> Пример: $N = 16384 \Rightarrow i = \log_2(16384) = 14$ бит

#### 📏 Важно: перевод из дюймов в пиксели

В задачах иногда указывается **физический размер изображения в дюймах** и **разрешение в DPI (dots per inch — точек на дюйм)**. Тогда:

$$
\text{Ширина в пикселях} = \text{Ширина в дюймах} \times \text{DPI}
$$
$$
\text{Высота в пикселях} = \text{Высота в дюймах} \times \text{DPI}
$$
> 🔹 Пример:  
> Изображение 8 дюймов × 6 дюймов при 300 DPI →  
> $W = 8 \times 300 = 2400$ пикселей,  
> $H = 6 \times 300 = 1800$ пикселей.
---
### 2. 🎵 Для аудио

**Формула объёма (в битах):**  
$$
V = t \times f \times i \times k
$$

- $t$ — длительность (в секундах)  
- $f$ — частота дискретизации (Гц)  
- $i$ — глубина кодирования (бит на отсчёт)  
- $k$ — количество каналов (1 — моно, 2 — стерео и т.д.)

---
### 3. 🎬 Для видео

**Формула объёма (в битах):**  
$$
V = T \times F \times V_{\text{кадра}}
$$

Где объём одного кадра:  
$$
V_{\text{кадра}} = W \times H \times i
$$

- $T$ — длительность (сек)  
- $F$ — частота кадров (кадр/с)  
- $W, H, i$ — как в изображениях

---
### 4. 📥 Для передачи данных

**Формула времени передачи:**  
$$
V = {S}\times{t}
$$

- $t$ — время (в секундах)  
- $V$ — объём данных (в битах)  
- $S$ — скорость передачи (бит/с)

---
### 5. 🧮 Для сжатия

**Коэффициент сжатия:**  
$$
k = \frac{V_{\text{исходный}}}{V_{\text{сжатый}}}
$$

**Процент сжатия:**  
$$
\text{Сжатие (\%)} = \left(1 - \frac{V_{\text{сжатый}}}{V_{\text{исходный}}}\right) \times 100\%
$$

> 💡 Пример: если файл сжался на 30% → $V_{\text{сжатый}} = 0.7 \times V_{\text{исходный}}$

---
## 📏 Таблица перевода единиц измерения объёма данных

> 💡 Используется в задачах на хранение и передачу мультимедийных данных.  
> Важно: **в информатике 1 Кбайт = 1024 байта**, а не 1000 — это критично для точных расчётов.

### 📊 Таблица перевода (в байтах)

| Единица       | В байтах           | В битах (×8)        |
|---------------|--------------------|----------------------|
| 1 бит         | 1/8 байта          | 1 бит                |
| 1 байт        | 1 байт             | 8 бит                |
| 1 Кбайт       | 1024 байта         | 8192 бит              |
| 1 Мбайт       | 1024 Кбайт         | 8 388 608 бит         |
| 1 Гбайт       | 1024 Мбайт         | 8 589 934 592 бит     |
| 1 Тбайт       | 1024 Гбайт         | 8 796 093 022 208 бит |

### 🔄 Быстрые переводы (для расчётов)

| Из            | В                 | Формула                     |
|---------------|-------------------|-----------------------------|
| биты          | байты             | ÷ 8                         |
| байты         | Кбайты            | ÷ 1024                      |
| Кбайты        | Мбайты            | ÷ 1024                      |
| Мбайты        | Гбайты            | ÷ 1024                      |
| Гбайты        | Тбайты            | ÷ 1024                      |
| Кбайты        | биты              | × 8192                      |
| Мбайты        | биты              | × 8 388 608                 |
| Гбайты        | биты              | × 8 589 934 592             |
### 🧮 Примеры

- 1 Мбайт = 1024 Кбайт = 1 048 576 байт = 8 388 608 бит  
- 512 Кбайт = 512 × 1024 = 524 288 байт = 4 194 304 бит  
- 2 Гбайт = 2 × 1024 = 2048 Мбайт = 2 097 152 Кбайт

### ⚠️ Важно

- В задачах ЕГЭ **всегда используется двоичная система**:  
  `1 Кбайт = 1024 байта`, `1 Мбайт = 1024 Кбайт`, и т.д.  
- В некоторых задачах может быть указано «в десятичной системе» — тогда 1 Кбайт = 1000 байт, но это **редкость**.  
- Если в условии не указано — считайте по двоичной системе (1024).

---
# Принцип решения

## Этап 1: Определить тип данных

- Изображение → используйте $V = W \times H \times i$  
- Аудио → используйте $V = t \times f \times i \times k$  
- Видео → используйте $V = T \times F \times V_{\text{кадра}}$  
- Передача → используйте $V = t \times S$  
- Сжатие → используйте $V_{\text{сжатый}} = V_{\text{исходный}} \times k$

## Этап 2: Выписать известные величины

Запишите все данные из условия — с единицами измерения.

## Этап 3: Подставить в формулу и вычислить

Используйте соответствующую формулу, подставьте значения, посчитайте.

## Этап 4: Перевести в нужные единицы

- Биты → байты → Кбайт → Мбайт → Гбайт  
- Секунды → минуты → часы  
- Убедитесь, что ответ в нужной единице — например, «в Мбайт», «в секундах», «целое число»

## Этап 5: Проверить логику

- Если объём увеличился после сжатия — ошибка.  
- Если время передачи отрицательное — ошибка.  
- Если количество файлов не целое — округлите вниз (если не указано иное).

---
## 💡 Советы 

- **Запишите формулы на листе** — они всегда одни и те же.  
- **Следите за единицами** — биты и байты, Кбайт и Мбайт, секунды и минуты.  
- **Не забывайте про сжатие** — оно может быть указано как «на 30%», «в 2 раза», «коэффициент 0.5» и т.д.  
- **Проверяйте, что вычисляете** — объём, время, количество, скорость?  
- **Если сомневаетесь — подставьте числа в формулу и посчитайте по шагам.**

---

## 📌 Примеры типичных ошибок

- Перепутать биты и байты → ответ в 8 раз больше или меньше.  
- Забыть про количество каналов в аудио → ответ в 2 раза меньше (для стерео).  
- Не учесть сжатие → ответ в 2–10 раз больше.  
- Использовать 1000 вместо 1024 → ответ на 2–3% меньше.

# Примеры

---