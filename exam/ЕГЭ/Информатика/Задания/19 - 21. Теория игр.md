---
aliases:
  - ege
  - exam
  - егэ
  - экзамен
tags:
  - ege
  - exam
date: 2026-01-13
---
# Описание

---
# Принцип решения

---
# Шаблон кода

---
# Примеры

---
# Описание

---

В этом типе заданий описывается **пошаговая игра двух игроков** — Пети и Вани — с чёткими правилами ходов и условиями победы. Игра ведётся с одной или двумя куч камней, количество которых изменяется по определённым правилам. Игроки ходят по очереди, первый ход делает Петя. Победителем считается тот, кто первым доведёт количество камней до заданного порога (например, ≥ 125, ≤ 30, ≤ 15 и т.д.).

Каждый ход игрока может включать:
- добавление фиксированного количества камней (например, +2, +4);
- убирание фиксированного количества камней (например, -3, -8);
- умножение или деление количества камней (например, ×2, ÷3 с округлением вниз или вверх).

Цель — найти **начальное количество камней S**, при котором выполняются определённые условия выигрышных стратегий для Пети или Вани, например:
- Петя не может выиграть за один ход, но Ваня может выиграть своим первым ходом при любом ходе Пети (задание 19);
- Петя не может выиграть за один ход, но может выиграть своим вторым ходом независимо от ходов Вани (задание 20);
- Ваня может выиграть первым ходом при любой игре Пети, но не имеет гарантированной выигрышной стратегии за один ход (задание 21).

📌 **Важно**: задачи требуют анализа **всех возможных ходов** и **результатов игры на несколько ходов вперёд**, что делает их идеальными для решения с помощью **рекурсивных функций** и **логических операций** (any, all).

---

# Принцип решения

## Общая логика подхода

Такие задачи решаются с помощью **рекурсивного перебора всех возможных ходов** с учётом того, кто ходит (Петя или Ваня), и количества оставшихся ходов. Основная идея — определить, при каком начальном S игрок имеет выигрышную стратегию, то есть может выиграть при любых ходах противника.

### Основные этапы решения:

1. **Определить условие победы**  
   Установить, при каком количестве камней игра завершается (например, ≥ 125 или ≤ 15). Это условие проверяется в начале рекурсивной функции.

2. **Реализовать рекурсивную функцию**  
   Функция `f(s, m)` принимает:
   - `s` — текущее количество камней;
   - `m` — количество оставшихся ходов (или номер хода, отсчитываемый от конца).

   Возвращаемое значение — логическое: `True`, если текущий игрок (при данном `m`) может выиграть, `False` — если нет.

3. **Учесть чётность хода**  
   Если `m % 2 == 0`, значит, ход делает Ваня (второй игрок), и он должен выиграть при **любом** ходе Пети → используется `all(...)`.  
   Если `m % 2 != 0`, значит, ход делает Петя (первый игрок), и он может выиграть, если **хотя бы один** ход ведёт к победе → используется `any(...)`.

4. **Реализовать возможные ходы**  
   Для текущего `s` сгенерировать все возможные следующие состояния (например, `s+2`, `s+4`, `s*2`) и рекурсивно проверить их.

5. **Найти нужные значения S**  
   Перебрать все возможные `S` в заданном диапазоне (например, от 1 до 124) и отобрать те, которые удовлетворяют условиям задач 19, 20, 21.

6. **Записать ответ**  
   Ответы оформляются в виде списка значений S в порядке возрастания, как требуется в условии.

## Почему такой подход эффективен?

Рекурсивный перебор с учётом логики "любой ход" / "хотя бы один ход" позволяет точно моделировать стратегию игроков. Это особенно важно в задачах, где нужно учесть **все возможные ответы противника**. Такой подход гарантирует, что найденные значения S действительно соответствуют условиям выигрышных стратегий.

---

# Шаблон кода

```python
def f(s, m):
    # Условие победы: если количество камней достигло порога
    if s >= 125:  # или <= 15, <= 30 — зависит от задачи
        return m % 2 == 0  # True, если победил текущий игрок (зависит от чётности m)
    if m == 0:  # если ходов больше нет, но победы нет — проигрыш
        return 0
    # Генерируем все возможные ходы
    h = [
        f(s + 2, m - 1),  # добавить 2
        f(s + 4, m - 1),  # добавить 4
        f(s * 2, m - 1),  # умножить на 2
        # f(s // 3, m - 1),  # делить на 3 (если есть в задаче)
        # f(s - 3, m - 1),  # убрать 3 (если есть в задаче)
    ]
    # Если ход Пети (нечётный m) — хотя бы один ход должен вести к победе
    if m % 2 != 0:
        return any(h)
    # Если ход Вани (чётный m) — все ходы должны вести к победе
    else:
        return all(h)

def main():
    # Задание 19: Петя не может выиграть за 1 ход, но Ваня может выиграть своим первым ходом при любом ходе Пети
    print(19, [s for s in range(1, 125) if not f(s, 1) and f(s, 2)])
    # Задание 20: Петя не может выиграть за 1 ход, но может выиграть своим вторым ходом (при любом ходе Вани)
    print(20, [s for s in range(1, 125) if not f(s, 1) and f(s, 3)])
    # Задание 21: Ваня может выиграть первым ходом при любой игре Пети, но не имеет гарантированной стратегии за 1 ход
    print(21, [s for s in range(1, 125) if not f(s, 2) and f(s, 4)])

if __name__ == '__main__':
    main()
```
