---
aliases:
  - ege
  - exam
  - егэ
  - экзамен
tags:
  - ege
  - exam
date: 2025-12-24
---
# Описание
## Работа с IP-адресами и масками сетей в терминологии TCP/IP

В этом типе задач рассматривается **структура IP-адреса и маски сети** в терминологии TCP/IP. Каждый IP-адрес и маска состоят из **четырёх октетов** (по 8 бит каждый), записанных в десятичной системе счисления — например, `192.168.1.1` или `255.255.255.0`.

Цель — **найти адрес сети, широковещательный адрес, диапазон допустимых адресов для хостов**, или **определить, какой IP-адрес может быть назначен компьютеру** — с учётом того, что:

- **Адрес сети** — получается в результате **поразрядной конъюнкции (И)** IP-адреса узла и маски сети.
- **Широковещательный адрес** — получается, если в адресе сети на месте нулей в маске поставить единицы.
- **Адрес сети и широковещательный адрес не могут быть назначены компьютеру** — они зарезервированы.

> ❗ Важно: задачи часто содержат «подводные камни» — например, «наибольший IP-адрес, который может быть назначен компьютеру», «сумма числовых значений октетов», «нечётное количество единиц в двоичной записи».

---
## 📚 Теория: IP-адресация и маски сетей в TCP/IP

> 💡 В этом разделе — всё, что нужно знать для решения задач ЕГЭ по информатике на тему IP-адресов и масок сетей. Без лишней воды — только ключевые понятия, формулы и правила.

### 1. 🖥️ Что такое IP-адрес?
**IP-адрес** — уникальный идентификатор устройства в сети. В IPv4 он состоит из **4 октетов** (по 8 бит), записанных в десятичной системе через точки: 192.168.1.1
Каждый октет — это число от 0 до 255 (потому что 8 бит = 2⁸ = 256 значений).

> 🔹 Пример: `192.168.1.1` = `11000000.10101000.00000001.00000001` в двоичной системе.

---

### 2. 🧭 Что такое маска сети?
**Маска сети** — это специальный IP-адрес, который показывает, **какая часть IP-адреса относится к сети**, а какая — к хосту (устройству в сети).
Маска состоит из **последовательности единиц слева и нулей справа** — например: 255.255.252.0 → 11111111.11111111.11111100.00000000

> 💡 Часто маску записывают в виде `/22` — это **CIDR-нотация**, где 22 — количество единиц в маске.  
> В задачах ЕГЭ маска обычно задаётся в виде `255.255.252.0`.

---
### 3. 🧮 Как найти адрес сети?
**Адрес сети** — это адрес, который определяет **саму сеть**, а не конкретное устройство.
#### Формула: 
Адрес сети = IP-адрес узла И маска сети

> 📌 Операция **«И» (поразрядная конъюнкция)** — выполняется побитово:  
> `1 И 1 = 1`, `1 И 0 = 0`, `0 И 1 = 0`, `0 И 0 = 0`.

#### Пример:
IP-адрес: `190.202.83.62`  
Маска: `255.255.252.0`

Переводим в двоичную систему:
IP:        10111110.11001010.01010011.00111110
Маска: 11111111.11111111.11111100.00000000
И:         10111110.11001010.01010000.00000000 → 190.202.80.0

✅ Адрес сети: `190.202.80.0`

---
### 4. 📢 Что такое широковещательный адрес?
**Широковещательный адрес** — это адрес, на который отправляются сообщения **всем устройствам в сети**.
#### Как найти: 
Широковещательный адрес = Адрес сети ИЛИ (НЕ маска)

> 📌 Операция **«ИЛИ» (поразрядная дизъюнкция)** — `1 ИЛИ 1 = 1`, `1 ИЛИ 0 = 1`, `0 ИЛИ 1 = 1`, `0 ИЛИ 0 = 0`.  
> Операция **«НЕ»** — инвертирует биты: `1 → 0`, `0 → 1`.

#### Пример:
Адрес сети: `190.202.80.0`  
Маска: `255.255.252.0`

Адрес сети: 10111110.11001010.01010000.00000000 
НЕ маска:    00000000.00000000.00000011.11111111 
ИЛИ:           10111110.11001010.01010011.11111111 → 190.202.83.255


✅ Широковещательный адрес: `190.202.83.255`

---
### 5. 🚫 Что нельзя назначать компьютеру?

- **Адрес сети** — зарезервирован для обозначения самой сети.
- **Широковещательный адрес** — зарезервирован для рассылки всем устройствам.

> ✅ Допустимые адреса для хостов: от **адрес сети + 1** до **широковещательный адрес – 1**.

#### Пример:
Адрес сети: `190.202.80.0`  
Широковещательный адрес: `190.202.83.255`  
→ Допустимые адреса: `190.202.80.1` — `190.202.83.254`

---

### 6. 🎯 Как найти наибольший/наименьший IP-адрес для хоста?

- **Наименьший**: `Адрес сети + 1`  
- **Наибольший**: `Широковещательный адрес – 1`

> 💡 В задачах часто просят найти **наибольший IP-адрес**, который может быть назначен компьютеру — это `Широковещательный адрес – 1`.

---
### 7. 🔢 Как найти сумму числовых значений октетов?

Просто сложить все 4 числа:
IP-адрес: 190.202.83.62 → 190 + 202 + 83 + 62 = 537

> 💡 В задачах часто требуется найти IP-адрес с **максимальной/минимальной суммой октетов**, или с **нечётной суммой**, или с **суммой, равной 31** и т.д.

---

### 8. 📊 Как посчитать количество единиц в двоичной записи IP-адреса?
Перевести IP-адрес в двоичную систему — и посчитать количество единиц.
### Пример:
IP-адрес: `190.202.83.62`

190 → 10111110 → 6 единиц 
202 → 11001010 → 4 единицы 
83 → 01010011 → 4 единицы 
62 → 00111110 → 5 единиц

✅ Всего: `6 + 4 + 4 + 5 = 19` единиц
 💡 В задачах часто требуется найти IP-адрес с **нечётным количеством единиц**, или с **максимальным количеством единиц** и т.д.

---

### 9. 🧩 Как работать с условиями?
Часто в задачах есть **дополнительные условия** — например:
- «наибольший IP-адрес, у которого количество единиц в двоичной записи нечётно»
- «IP-адрес, сумма октетов которого равна 31»
- «IP-адрес, который не может быть назначен компьютеру»

#### Алгоритм:
1. Найти диапазон допустимых адресов (от `сеть + 1` до `широковещательный – 1`).
2. Перебрать все адреса в этом диапазоне.
3. Для каждого адреса проверить условия.
4. Выбрать нужный — например, наибольший, с нечётным количеством единиц, с суммой 31 и т.д.

> 💡 Если диапазон большой — можно не перебирать все, а **подбирать с конца** — начиная с `широковещательный – 1` и идя вниз.

---
### 10. 🧭 Как определить, какой IP-адрес может быть назначен компьютеру?
Любой IP-адрес в диапазоне `сеть + 1` — `широковещательный – 1`.

---
### 📌 Краткий чек-лист для решения задач
✅ Перевести IP-адрес и маску в двоичную систему.  
✅ Найти адрес сети: `IP И маска`.  
✅ Найти широковещательный адрес: `сеть ИЛИ (НЕ маска)`.  
✅ Определить диапазон: `сеть + 1` — `широковещательный – 1`.  
✅ Если нужно — посчитать сумму октетов или количество единиц.  
✅ Если нужно — найти наибольший/наименьший адрес с нужным свойством.  
✅ Записать ответ — как требуется: IP-адрес, сумма октетов, количество единиц и т.д.

---
## Теория модуля ipaddress
Модуль `ipaddress` в Python предоставляет удобные инструменты для работы с IP-адресами и сетями. Для задач ЕГЭ, где нужно находить адрес сети, широковещательный адрес, диапазон хостов и фильтровать адреса по условиям, ключевым объектом является `ip_network`.

---
### 📦 `ipaddress.ip_network`

Это основной объект — представляет **IP-сеть** (IPv4 или IPv6). Создаётся из строки вида `"IP-адрес/маска"`.
#### Конструктор:

```python
network = ipaddress.ip_network("190.202.83.62/255.255.252.0", strict=False)
```
- `strict=False` — позволяет передавать IP-адрес узла (не обязательно адрес сети). Без этого — ошибка, если IP не совпадает с сетью.
- Возвращает объект типа `IPv4Network` (если IPv4) или `IPv6Network` (если IPv6).

---
### 🔑 Основные свойства объекта `network`
#### 1. `network.network_address`
- Тип: `IPv4Address` или `IPv6Address`
- Что это: адрес сети — самый первый адрес в диапазоне.
- Пример: `190.202.80.0`
#### 2. `network.broadcast_address`
- Тип: `IPv4Address` или `IPv6Address`
- Что это: широковещательный адрес — самый последний адрес в диапазоне.
- Пример: `190.202.83.255`
#### 3. `network.hosts()`
- Тип: генератор (итератор)
- Что это: возвращает **все адреса, которые можно назначить хостам** — то есть, **без адреса сети и широковещательного**.
- Возвращает объекты типа `IPv4Address`.
- Пример: `190.202.80.1`, `190.202.80.2`, ..., `190.202.83.254`
> 💡 Чтобы получить список, оберните в `list()`:  
> `hosts = list(network.hosts())`

#### 4. `network.num_addresses`

- Тип: `int`
- Что это: общее количество адресов в сети — включая адрес сети и широковещательный.
- Пример: для маски `/22` → `2^(32-22) = 1024` адреса.

#### 5. `network.prefixlen`

- Тип: `int`
- Что это: длина префикса в битах — количество единиц в маске.
- Пример: для маски `255.255.252.0` → `22` (потому что `11111111.11111111.11111100.00000000` — 22 единицы).

#### 6. `network.netmask`

- Тип: `IPv4Address`
- Что это: маска сети в виде IP-адреса.
- Пример: `255.255.252.0`

#### 7. `network.hostmask`

- Тип: `IPv4Address`
- Что это: маска хоста — инверсия маски сети.
- Пример: для `255.255.252.0` → `0.0.3.255`

---

### 🧩 Объект `IPv4Address` (возвращается из `hosts()`, `network_address`, `broadcast_address`, ip_address)
Это объект, представляющий **один IP-адрес**.
#### Основные свойства и методы:
##### 1. `host.packed`
- Тип: `bytes` (для IPv4 — 4 байта)
- Что это: байтовое представление адреса.
- Пример: `b'\xbe\xca\x50\x01'` для `190.202.80.1`
- Полезно для подсчёта суммы октетов: `sum(host.packed)`
##### 2. `str(host)`
- Тип: `str`
- Что это: строковое представление адреса в формате `xxx.xxx.xxx.xxx`
- Пример: `str(host)` → `"190.202.80.1"`
##### 3. `host.compressed`
- Тип: `str`
- Что это: сжатая форма адреса (для IPv6 — полезно, для IPv4 — то же, что `str(host)`)
##### 4. `host.exploded`
- Тип: `str`
- Что это: развёрнутая форма адреса — для IPv4 — то же, что `str(host)`
##### 5. Операторы сравнения:
- `host1 < host2`, `host1 == host2`, `host1 > host2` — работают как числа.

---
### 📊 Как использовать в задачах
- Чтобы получить **адрес сети** — `network.network_address`
- Чтобы получить **широковещательный адрес** — `network.broadcast_address`
- Чтобы получить **все хосты** — `network.hosts()`
- Чтобы получить **сумму октетов** — `sum(host.packed)`
- Чтобы получить **количество единиц в двоичной записи** — `''.join(format(octet, '08b') for octet in host.packed).count('1')`
- Чтобы получить **строковое представление** — `str(host)`

---
### 💡 Важно
- Все объекты `IPv4Address` и `IPv4Network` — **неизменяемые** (immutable).
- `hosts()` — это **генератор**, а не список — если нужно несколько раз перебирать — оберните в `list()`.
- `strict=False` — **обязателен**, если IP-адрес не является адресом сети.
- `host.packed` — **ключевое свойство** для подсчёта суммы и двоичной записи. 
---
# Принцип решения
## 🧠 Общая логика подхода

Такие задачи требуют **вычисления адреса сети, широковещательного адреса, диапазона допустимых хостов**, а также **поиска IP-адреса, удовлетворяющего дополнительным условиям** — например, «наибольший», «с нечётным количеством единиц в двоичной записи», «сумма октетов равна 31» и т.д.

Решение строится на **использовании стандартного модуля Python `ipaddress`**, который позволяет:

- Создавать IP-сети из IP-адреса и маски.
- Получать адрес сети, широковещательный адрес, диапазон хостов.
- Перебирать все адреса в сети.
- Проверять условия для каждого адреса.

> 💡 Важно: модуль `ipaddress` работает с IPv4 и IPv6, но в задачах ЕГЭ — только IPv4.

---
## 📋 Основные этапы решения

### 1. Импортировать модуль `ipaddress`
Это стандартный модуль Python — не требует установки.

---
### 2. Создать объект сети из IP-адреса и маски
Используется формат `IP-адрес/маска`, с параметром `strict=False`, чтобы модуль не требовал, чтобы IP-адрес был адресом сети — ведь в задачах даётся адрес узла.

---
### 3. Получить ключевые адреса
- **Адрес сети** — начало диапазона.
- **Широковещательный адрес** — конец диапазона.

Эти адреса **нельзя назначать компьютеру**.

---
### 4. Получить диапазон допустимых адресов для хостов
Список всех адресов, кроме адреса сети и широковещательного. Это — все возможные адреса для устройств в сети.

---
### 5. Перебрать адреса и проверить условия
- Если нужно найти **наибольший IP-адрес** — берётся последний в списке.
- Если нужно найти адрес с **нечётным количеством единиц в двоичной записи** — для каждого адреса вычисляется количество единиц в его 32-битном представлении.
- Если нужно найти адрес с **суммой октетов равной 31** — для каждого адреса суммируются значения четырёх октетов.
- И т. д.

---
### 6. Выбрать нужный адрес и вывести ответ
- Ответ может быть:
  - IP-адресом (в виде строки).
  - Суммой октетов.
  - Количеством единиц в двоичной записи.

Всё зависит от условия задачи.

---
# Шаблон кода


---
# Примеры

---