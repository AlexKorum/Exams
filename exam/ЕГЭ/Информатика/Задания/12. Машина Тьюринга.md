---
aliases:
  - ege
  - exam
  - егэ
  - экзамен
tags:
  - ege
  - exam
date: 2025-12-24
---
# Описание

## Машина Тьюринга (МТ). Работа с лентой, состояниями и командами

В этом типе задач рассматривается **исполнитель Машина Тьюринга (МТ)** — абстрактный автомат, который работает с **бесконечной лентой**, разделённой на ячейки. В каждой ячейке — один символ из алфавита, включая **специальный пустой символ** (обычно обозначается как `λ` или `␣`).

МТ имеет:

- **Головку**, которая может **читать и записывать символы** в текущей ячейке.
- **Множество состояний** — в каждом такте МТ находится в одном из них (обычно `q₀`, `q₁`, `q₂`, ...).
- **Программу работы**, заданную в виде таблицы: для каждой пары «состояние — символ» указана команда.

Команда состоит из трёх элементов:

1. **Символ для записи** в текущую ячейку (может совпадать с текущим).
2. **Направление сдвига** головки: `L` — влево, `R` — вправо, `N` — не сдвигаться, `S` — завершить работу.
3. **Новое состояние** головки после выполнения команды.

Цель — **выполнить программу МТ**, начиная с заданного состояния и начальной конфигурации ленты, и **определить, какое число (или строка) останется на ленте в результате или была до её исполнения**

> ❗ Важно: задачи часто содержат «подводные камни» — например, «головка в начальный момент в ближайшей слева от последовательности ячейке», «лента заполнена пустыми символами», «запись символа происходит до сдвига».

---

## Типы задач (по содержанию)

1. **Выполнить программу МТ и найти число на ленте** — например, «определите результат работы программы».
2. **Найти число на ленте после выполнения программы** — при заданной начальной конфигурации.
3. Найдите количество элементов до исполнения программы — обычно задана результирующая строка после исполнения.

# Принцип решения
## 🧠 Общая логика подхода

Задачи на Машину Тьюринга требуют **пошагового моделирования работы автомата** — от начального состояния и начальной конфигурации ленты до завершения работы (команда `S`). Цель — либо **найти результат на ленте**, либо **восстановить начальную конфигурацию** по результату.

Решение строится на **строгом следовании таблице команд** — для каждой пары «состояние — символ» выполняется команда, состоящая из трёх элементов: запись символа, сдвиг головки, переход в новое состояние.

> 💡 Важно:  
> - Команда выполняется **в один такт**.  
> - Запись символа происходит **до сдвига**.  
> - Головка в начальный момент может быть **не над первым символом**, а в ближайшей слева/справа ячейке.  
> - Лента бесконечна, и ячейки, не содержащие символов, заполнены **пустым символом** (обычно `λ` или `␣`).

---
## 📋 Два основных типа задач

### 🔹 Тип 1: **Найти результат работы программы (что останется на ленте)**

**Условие**: дана начальная конфигурация ленты, начальное состояние, программа МТ.  
**Цель**: выполнить программу до завершения (`S`) и определить, что записано на ленте.

#### Этапы решения:

1. **Зафиксировать начальную конфигурацию**:
   - Лента: какая последовательность символов записана.
   - Головка: в какой ячейке находится (например, «в ближайшей слева от последовательности»).
   - Состояние: `q₀` (обычно).

2. **Выполнять команды по таблице**:
   - На каждом шаге:
     - Определить текущее состояние и символ под головкой.
     - Найти команду в таблице.
     - Выполнить: записать символ, сдвинуть головку, перейти в новое состояние.
     - Если команда `S` — остановиться.

3. **Следить за состоянием ленты и положением головки**:
   - Можно рисовать ленту на бумаге или в тексте — например:  
     `... λ λ 1 0 1 λ λ ...` — и отмечать положение головки (например, `1[0]1` — головка над `0`).

4. **После завершения — прочитать результат**:
   - Обычно это **последовательность цифр**, которую нужно перевести в десятичную систему.
   - Учитывать, что ведущие нули могут быть удалены — если в условии сказано «без ведущих нулей».

---

### 🔹 Тип 2: **Восстановить начальную конфигурацию по результату**

**Условие**: дана программа МТ, результат на ленте, и сказано, что программа завершилась.  
**Цель**: определить, какая начальная конфигурация (лента + положение головки) могла привести к такому результату.

#### Этапы решения:

1. **Анализировать программу на обратимость**:
   - Можно ли «откатить» шаги? Если команда `L` — значит, предыдущий шаг был `R`, и т.д.
   - Если программа содержит `S` — значит, последний шаг — завершение, и его нельзя откатить.

2. **Попробовать «откатить» шаги от результата**:
   - Начать с конечного состояния и конечной ленты.
   - Для каждого шага — найти, какая команда могла привести к текущему состоянию.
   - Учитывать, что запись символа происходит до сдвига — значит, предыдущий символ в ячейке мог быть другим.

3. **Восстановить начальную ленту и положение головки**:
   - Это может быть не единственное решение — но в задачах ЕГЭ обычно есть только одно.

4. **Проверить, что восстановленная начальная конфигурация действительно приводит к заданному результату**:
   - Выполнить программу вперёд — если получится тот же результат — значит, решение верно.

---

## 💡 Советы

- **Не пытайтесь угадать** — выполняйте шаги по таблице.
- **Рисуйте ленту** — даже на бумаге, с отмеченной головкой.
- **Следите за состоянием** — не перепутайте `q₁` и `q₂`.
- **Учитывайте пустые символы** — если головка сдвинулась в пустую ячейку — символ `λ`.
- **Если команда `S` — остановка** — не выполняйте следующие шаги.
- **Проверяйте результат** — если на ленте `101`, это 5 в десятичной системе.

---
# Примеры

---