---
aliases:
  - ege
  - exam
  - егэ
  - экзамен
tags:
  - ege
  - exam
date: 2025-12-23
---
# Описание
## Исполнитель Черепаха. Алгоритмическое движение на плоскости

В этом типе задач рассматривается **исполнитель Черепаха**, который перемещается по плоскости с декартовой системой координат. В начальный момент Черепаха находится в начале координат и направлена вдоль положительного направления оси ординат (вверх).

Исполнитель умеет выполнять команды:

- **Вперёд n** — перемещение на n единиц в текущем направлении.
- **Назад n** — перемещение на n единиц в противоположном направлении.
- **Направо m** — поворот на m градусов по часовой стрелке.
- **Налево m** — поворот на m градусов против часовой стрелки.
- **Поднять хвост / Опустить хвост** — управление рисованием линий при движении.

Команды могут повторяться с помощью конструкции `Повтори k [Команда1 Команда2 ... КомандаS]`.

Цель — ответить на вопрос поставленный в задаче, пользуясь чертежом полученным после работы программы исполнителя.

---
# Принцип решения
## Общая логика подхода

Такие задачи требуют **моделирования движения исполнителя** по плоскости с учётом поворотов, перемещений и режимов рисования («хвост опущен» / «хвост поднят»). Цель — восстановить траекторию, определить фигуры, которые нарисованы, и найти количество целочисленных точек внутри их пересечения (границы не учитываются).

Решение строится на **симуляции движения Черепахи** с помощью модуля `turtle`, который позволяет:

- Задать начальное положение и направление.
- Выполнять команды движения и поворота.
- Отслеживать координаты точек, через которые проходит Черепаха при опущенном хвосте.
- Визуализировать траекторию (опционально, для проверки).
- Определить области, ограниченные линиями, и посчитать точки внутри пересечения.

---

## Основные этапы решения

### 1. Инициализировать Черепаху

- Установить начальное положение (0, 0) и направление (вправо — по умолчанию в turtle).
- Настроить режим рисования: хвост поднят или опущен — в зависимости от команд в программе.

> 💡 Модуль `turtle` автоматически отслеживает координаты при движении — можно сохранять их в список, если нужно анализировать траекторию.

---

### 2. Выполнить программу Черепахи

- Прочитать последовательность команд, включая циклы (`Повтори k [...]`).
- Для каждой команды:
  - Если это `Вперёд n` или `Назад n` — переместить Черепаху на n единиц в текущем направлении.
  - Если это `Направо m` или `Налево m` — повернуть Черепаху на m градусов.
  - Если это `Поднять хвост` или `Опустить хвост` — переключить режим рисования.
  - Если это `Повтори k [...]` — выполнить блок команд k раз.

> 📌 Важно: команды внутри цикла могут содержать вложенные циклы — нужно корректно обрабатывать вложенность.

---

### 3. Отслеживать траекторию и фигуры

- Сохранять координаты всех точек, через которые прошла Черепаха **при опущенном хвосте** — это и есть линии, ограничивающие фигуры.
- Определить, какие фигуры нарисованы — например, прямоугольники, треугольники, многоугольники.
- Если нарисовано несколько фигур — найти их **пересечение**.

> 💡 Для определения пересечения можно использовать геометрические методы или библиотеки (например, `shapely`), но в задачах ЕГЭ часто достаточно визуального анализа или простого перебора точек.

---

### 4. Построить координатную сетку

- Перебрать все точки с целочисленными координатами в разумном диапазоне (например, от -100 до 100 по x и y).
- Каждую точку нарисовать на плоскости, так чтобы они покрывали нарисованные фигуры

---
### 5. Решить задачу

- Решить поставленную задачу в рамках геометрических задач
- Чаще всего нужно найти количество точек находящихся внутри пересечения/объединения фигур

---
# Шаблон кода

---
# Примеры

---