---
aliases:
  - ege
  - exam
  - егэ
  - экзамен
tags:
  - ege
  - exam
date: 2025-12-23
---
# Описание

## **Алгоритмы преобразования чисел между системами счисления**

В этом типе задач задаётся **алгоритм**, который преобразует входное натуральное число N в выходное число R. Алгоритм включает:

- **Перевод числа N в другую систему счисления** (не обязательно двоичная  — может быть троичная, четверичная, пятеричная и т.д.).
- **Обработку полученной записи по определённому правилу**, зависящему от свойств числа N (например, делится ли оно на основание системы, или сумма цифр, или чётность и т.п.).
- **Дописывание новых цифр** к записи — справа, слева или в конец — по заданному правилу.
- **Перевод результата обратно в десятичную систему** — это и есть искомое число R.

Цель — **найти минимальное (или максимальное) число R (или N)**, удовлетворяющее дополнительным условиям (например, «нечётное», «больше 208», «кратно 5» и т.д.).

---
# Принцип решения
## Общая логика подхода

Такие задачи требуют **пошагового применения заданного алгоритма** к входному числу N, чтобы получить выходное число R. Алгоритм включает перевод в другую систему счисления, обработку записи по условию, и обратный перевод в десятичную систему.

Поскольку алгоритм детерминирован — **можно перебрать значения N**, применить к каждому алгоритм, получить R (или N), и отобрать те, которые удовлетворяют дополнительным условиям (например, «нечётное», «больше 208», «кратно 5» и т.д.).

Цель — найти **минимальное (или максимальное) значение R (или N)**, удовлетворяющее всем условиям.

## Основные этапы решения

### 1. Понять структуру алгоритма

Разобрать, что происходит на каждом шаге:

- В какую систему переводится число N? (например, троичную, двоичную, четверичную и т.д.)
- Какие условия применяются к N или к его записи? (например, делится ли на основание, чётная ли сумма цифр и т.п.)
- Что дописывается к записи? (например, последние цифры, сумма цифр, умноженная на основание и переведённая в ту же систему)
- В какую систему переводится результат? (обычно — в десятичную)

---
### 2. Применить алгоритм к нескольким значениям N

Выбрать небольшой диапазон значений N (например, от 1 до 100) и вручную или с помощью калькулятора:

- Перевести N в нужную систему счисления.
- Применить правило обработки (например, если делится на 3 — дописать последние две цифры; если нет — вычислить сумму цифр, умножить на 3, перевести в ту же систему и дописать).
- Получить новую запись — это троичная (или другая) запись числа R.
- Перевести R обратно в десятичную систему.

> 💡 Это помогает понять, как работает алгоритм, и проверить, нет ли ошибок в понимании.

---
### 3. Перебрать значения N в нужном диапазоне

Поскольку задача требует найти **минимальное (или максимальное)  R (или N)**, удовлетворяющее условиям — нужно:

- Перебирать N по возрастанию (или убыванию) (например, от 1 до 1000 или больше, если нужно).
- Для каждого N применять алгоритм и получать R.
- Проверять, удовлетворяет ли R дополнительным условиям (например, R > 208 и R — нечётное).
- Сохранять все подходящие R.

---
### 4. Выбрать итоговый ответ

Из всех подходящих R (или N) выбрать **наименьшее** (или наибольшее, в зависимости от условия) и записать его в десятичной системе.

---
# Шаблон кода

## 📌 Функции перевода

### 1. `dec_to_base(n: int, b: int) -> str`

```python
def dec_to_base(n: int, b: int) -> str:  
    """  
    Переводит число n из десятичной системы счисления, в систему с основанием b    
    :param n: Целое число    
    :param b: Натуральное число >= 2    
    :return: Строка с результатом перевода  
    """
    from string import digits, ascii_uppercase  
    alphabet = digits + ascii_uppercase  
    result = []  
    while n > 0:  
        result.append(alphabet[n % b])  
        n //= b  
    return ''.join(reversed(result))
```

**Зачем нужна?**  
Чтобы перевести число из десятичной системы в систему с основанием `b` — например, в троичную, двоичную, шестнадцатеричную и т.д.

**Почему именно так?**

- Используется **остатки от деления** — это стандартный алгоритм перевода.
- `n % b` — даёт последнюю цифру в новой системе.
- `n //= b` — сдвигаемся к следующей цифре.
- `alphabet = digits + ascii_uppercase` — позволяет работать с системами с основанием до 36 (0–9, A–Z).
- `reversed(result)` — потому что цифры мы получаем с конца, а записывать нужно слева направо.

> ✅ Пример: `dec_to_base(8, 3)` → `'22'` — потому что 8 = 2×3¹ + 2×3⁰.

---
### 2. `base_to_int(s: str, b: int) -> int`
```python
def base_to_int(s: str, b: int) -> int:  
    """  
    Перевод из системы счисления b, в десятичную систему счисления 
    :param s: Строка с числом в системе счисления b    
    :param b: Основание исходной системы счисления    
    :return: Целое число  
    """    
    return sum(int(el, 36) * b ** i for i, el in enumerate(reversed(s)))
```

**Зачем нужна?**  
Чтобы перевести число из системы с основанием `b` обратно в десятичную — например, `'22110'` из троичной → `228`.

**Почему именно так?**

- `reversed(s)` — потому что в числе старшие разряды слева, а в формуле мы начинаем с младшего (0-го) разряда.
- `int(el, 36)` — позволяет обрабатывать цифры от 0 до 9 и буквы A–Z (например, `'A'` → 10, `'Z'` → 35).
- `b ** i` — это степень основания, соответствующая позиции цифры.
- `sum(...)` — складываем все разряды с учётом их веса.

> ✅ Пример: `base_to_int('22', 3)` → `2×3¹ + 2×3⁰ = 8`.

## Общее решение задачи
```python
# настройки системы счисления
base = 3

# переменная сбора ответа  
result = []

# перебор чисел N  
for N in range(1, 1000):  
	# конвертация числа N в систему счисления base
    mN = dec_to_base(N, base)  
    
    # проверка условий для задачи
    # !!! Условия из задачи
    if N % 3 == 0:  
        mN += mN[-2:]  
    else:  
        mN += str(dec_to_base(sum(map(int, mN)) * 3, base))  
    
    # конвертация числа из системы счисления base в десятичную
    R = base_to_int(mN, base)  
    
    # определение подходящих чисел
    # !!! Условия из задачи
    if R > 208 and R % 2 != 0:  
        result.append(R)  
        
# получение ответа
# !!! Сам ответ также определчяется вопросом задачи
print(min(result))
```

 >⚠️ Код решения задачи приведен в качестве примера и дан для ознакомления. Не является полным кодом решения любой задачи

---
# Примеры

---