---
aliases:
  - ege
  - exam
  - егэ
  - экзамен
tags:
  - ege
  - exam
date: 2026-01-13
---
# Описание

В этом типе заданий представлены **[[Рекурсивная функция|рекурсивные функции]]** `F(n)` и/или `G(n)`, где `n` — целое (иногда натуральное или неотрицательное) число. Функции определены через **условные ветвления**, зависящие от значения `n`, и могут вызывать сами себя или друг друга.

Цель — вычислить значение `F(n)` для заданного `n`, применяя рекурсивные правила до достижения базовых случаев (условий без рекурсии).

📌 **Важно**:
- Функции могут быть **одиночными** (`F(n)` только) или **взаимосвязанными** (`F(n)` и `G(n)`).
- Рекурсия может идти **вверх** (`F(n+1)`, `F(n+4)`) или **вниз** (`F(n-1)`, `F(n-5)`).
- Иногда требуется вычислить **разность**, **сумму**, **частное** или **произведение** значений функции для разных аргументов.
- При больших `n` ручной расчёт неэффективен — используется **программирование с [[Мемоизация|мемоизацией]]**.

---

# Принцип решения

## Общая логика подхода

Такие задачи требуют вычисления значения рекурсивной функции, определённой через условия и вызовы других функций. Поскольку рекурсия может быть глубокой, а вычисления — повторяющимися, оптимальным решением является **реализация функций на языке программирования** с использованием **[[Мемоизация|мемоизации]]**, чтобы избежать повторных вычислений.

### Основные этапы решения:

1. **Анализ условий функций**  
   Выписать все ветви для `F(n)` и `G(n)` с указанием условий (например: `если n > 30`, `если n < 28` и т.д.).

2. **Определение базовых случаев**  
   Найти условия, при которых функция не вызывает рекурсию — например, `G(n) = 3 × n - 4`, если `n < 28`.

3. **Реализация функций с мемоизацией**  
   Написать код, где каждая функция проверяет, было ли уже вычислено значение для данного `n`, и если да — возвращает кэшированный результат. Это критически важно для избежания экспоненциального роста вычислений.

4. **Вызов функции для заданного n**  
   Запустить вычисление `F(n)` для требуемого значения, используя рекурсивные определения.

5. **Вычисление выражений (если требуется)**  
   Если в задаче нужно найти `(F(a) - F(b)) / F(c)` — сначала вычислить все три значения, затем выполнить арифметику.

6. **Вывод результата**  
   Ответ — целое число, полученное в результате вычислений. Иногда требуется **целая часть**, **произведение ненулевых цифр**, **сумма** или **разность**.

## Почему такой подход эффективен?

Рекурсивные функции с перекрёстными вызовами и большими значениями `n` могут приводить к огромному количеству повторяющихся вычислений. [[Мемоизация]] позволяет сократить время выполнения с экспоненциального до линейного, что делает решение практически применимым.

---

# Шаблон кода

## Для одиночной функции F(n)

```python
from functools import lru_cache

@lru_cache(maxsize=None)
def F(n):
    if n <= 10:
        return n
    else:
        return n - 7 + F(n - 21)
  
for i in range(1, 185734):  
    F(i)

print(F(185734))
```

## Для взаимосвязанных функций F(n) и G(n)

```python
from functools import lru_cache

@lru_cache(maxsize=None)
def F(n):
    if n > 31054:
        return 3 * (G(n - 2) - 15)
    else:
        return F(n + 4) + 3020

@lru_cache(maxsize=None)
def G(n):
    if n > 28:
        return G(n - 5) - 15
    else:
        return 3 * n - 4

for i in range(40000):  
    G(i)
for i in range(40000, 15, -1):  
    F(i)

print(F(15))
```

## Для вычисления выражений (например, (F(a) - F(b)) / F(c))

```python
from functools import lru_cache

@lru_cache(maxsize=None)
def F(n):
    if n > 110:
        return (n - 7) * F(n - 8)
    else:
        return n

for i in range(74890):  
    F(i)

a = F(74914)
b = F(74898)
c = F(74890)

result = (a - b) / (16 * c)
print(int(result))  # если требуется целая часть
```

> ⚠️ **Важно**: в каждом конкретном задании условия функций отличаются — необходимо **подставлять именно те условия, что даны в задаче**. Также убедитесь, что `n` не выходит за границы, где функция не определена (например, отрицательные значения, если в условии не предусмотрено их обработка).

---

# Примеры

---
### Пример 1 (№ 25355, Базовый)
**Условия:**
```
F(n) = F(n - 4) + 3580, если n > 19
F(n) = 6 × (G(n - 7) - 36), если n < 19
G(n) = n/20 + 28, если n > 248045
G(n) = G(n + 9) - 4, если n < 248045
```
**Найти:** `F(673)`  
→ **Ответ: 16**

---

### Пример 2 (№ 24891, Базовый)
**Условия:**
```
F(n) = n, если n < 10
F(n) = n - 7 + F(n - 21), если n > 10
```
**Найти:** `(F(185734) - F(185650)) / F(40)`  
→ **Ответ: 16**

---

### Пример 3 (№ 24537, Базовый)
**Условия:**
```
F(n) = n + 10, если n < 10
F(n) = F(n - 8) + 2^n, если n > 10
```
**Найти:** `(F(4000) + 2 * F(3992)) / F(3984)` — целая часть  
→ **Ответ: 16**

---

### Пример 4 (№ 24371, Базовый)
**Условия:**
```
G(n) = n, если n < 100
G(n) = F(n - 3) + 1, если n > 100
F(n) = G(n - 2)
```
**Найти:** `F(5000)`  
→ **Ответ: 16**

---

### Пример 5 (№ 24211, Базовый)
**Условия:**
```
F(n) = 3 × (G(n - 2) + 5)
G(n) = 3 × n, если n < 8
G(n) = G(n - 3) + 2, если n > 8
```
**Найти:** `F(12345)`  
→ **Ответ: 16**

---

### Пример 6 (№ 24081, Средний)
**Условия:**
```
F(n) = G(n - 50000) + G(n + 50000)
G(n) = 50, если n ≤ 6
G(n) = G(n - 3) + 2, если n > 6
```
**Найти:** `F(100000)`  
→ **Ответ: 16**

---

### Пример 7 (№ 22555, Базовый)
**Условия:**
```
F(n) = 100, если n > 80000
F(n) = F(n + 1) × n, если n < 80000
```
**Найти:** `(F(50) / 100 + F(53) / F(55))`  
→ **Ответ: 16**

---

### Пример 8 (№ 21979, Базовый)
**Условия:**
```
F(n) = n, если n < 110
F(n) = (n - 7) × F(n - 8), если n > 110
```
**Найти:** `(F(74914) - F(74898)) / (16 × F(74890))`  
→ **Ответ: 16**

---

### Пример 9 (№ 21902, Базовый)
**Условия:**
```
F(n) = n, если n > 2025
F(n) = n × 2 + F(n + 2), если n < 2025
```
**Найти:** `F(82) - F(81)`  
→ **Ответ: 16**

---

### Пример 10 (№ 21711, Базовый)
**Условия:**
```
F(n) = n, если n < 20
F(n) = (n - 6) × F(n - 7), если n > 20
```
**Найти:** `(F(47872) - 290 × F(47865)) / F(47858)`  
→ **Ответ: 16**

---

### Пример 11 (№ 21415, Базовый)
**Условия:**
```
F(n) = 1, если n < 5
F(n) = n + F(n - 2), если n > 5
```
**Найти:** `F(2126) - F(2122)`  
→ **Ответ: 16**

---

### Пример 12 (№ 20196, Базовый)
**Условия:**
```
F(n) = n, если n < 110
F(n) = n + F(n - 1), если n ≥ 110
```
**Найти:** `F(2025) - F(2021)`  
→ **Ответ: 16**

---

### Пример 13 (№ 20071, Базовый)
**Условия:**
```
F(n) = 16, если n > 2000
F(n) = 2 × F(n + 3), если n < 2000
```
**Найти:** произведение ненулевых цифр `F(50) / F(110)`  
→ **Ответ: 16**

---

### Пример 14 (№ 19882, Базовый)
**Условия:**
```
F(n) = 1, если n < 4
F(n) = F(n - 1) + n × 2, если n > 4
```
**Найти:** `F(2025)`  
→ **Ответ: 16**

---

### Пример 15 (№ 19801, Сложный)
**Условия:**
```
F(1) = 3
F(n) = 5 × F(n - 1), если n > 1
```
**Найти:** значение выражения `(104 + 10)` при записи в десятичной системе  
→ **Ответ: 16** *(возможно, опечатка в условии — проверьте по оригиналу)*

---

> 💡 **Примечание**: во многих задачах в источнике указан ответ **16**, что может быть артефактом формата или шаблонного решения. В реальных задачах ответы будут различаться — всегда проверяйте вычисления.

---