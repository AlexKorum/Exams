---
aliases:
  - ege
  - exam
  - егэ
  - экзамен
tags:
  - ege
  - exam
date: 2025-12-24
---
# Описание
## Кодирование серийных номеров. Минимизация мощности алфавита или длины кода

В этом типе задач рассматривается **присвоение серийных номеров изделиям** — каждому изделию присваивается уникальный номер, состоящий из символов определённого алфавита. Номера хранятся в базе данных, где каждый номер занимает **одинаковое и минимально возможное целое число байт**.

Цель — **определить минимально возможную мощность алфавита** (количество символов в алфавите), при которой можно закодировать заданное количество номеров, или **минимально возможную длину серийного номера**, при заданной мощности алфавита и объёме памяти.

> ❗ Важно: задачи часто содержат «подводные камни» — например, «минимально возможное целое число байт», «не менее 7 Мбайт», «мощность алфавита» — это количество символов, а не бит на символ.

---
## Типы задач (по содержанию)

1. **Определить минимальную мощность алфавита** — при заданной длине номера и объёме памяти.
2. **Определить минимальную длину номера** — при заданной мощности алфавита и объёме памяти.
3. **Определить минимальный объём памяти** — при заданной длине номера и мощности алфавита.
4. **Определить количество номеров** — при заданной длине, мощности алфавита и объёме памяти.

---
# Принцип решения
## 🧠 Общая логика подхода

Такие задачи требуют **определить минимальную мощность алфавита** (количество символов) или **минимальную длину серийного номера**, при которых можно закодировать заданное количество уникальных номеров, учитывая, что:

- Каждый символ кодируется **одинаковым и минимально возможным целым числом бит**.
- Каждый номер занимает **одинаковое и минимально возможное целое число байт**.
- Объём памяти, необходимый для хранения всех номеров, **не менее заданного значения** (например, не менее 7 Мбайт).

Решение строится на **расчёте минимального количества бит, необходимого для кодирования одного номера**, и **переводе этого количества в байты** — с округлением вверх до целого числа.

> 💡 Важно: **каждый номер — это строка символов**, и каждый символ кодируется **одинаковым числом бит** — это ключевое условие.

---
## 📐 Основные формулы

### 1. Минимальное количество бит на символ

Если мощность алфавита — $N$ (количество символов), то минимальное количество бит на символ:

$$
i = \lceil \log_2 N \rceil
$$

> 📌 **Объяснение**:  
> Чтобы закодировать $N$ символов, нужно как минимум $i$ бит, таких что $2^i \geq N$.  
> Так как биты — целые, округляем вверх.  
>  
> 🔹 Пример: $N = 10$ → $\log_2 10 \approx 3.32$ → $i = 4$ (потому что $2^4 = 16 \geq 10$).

---

### 2. Количество бит на один номер

Если длина номера — $L$ символов, то:

$$
B = L \times i
$$

> 📌 **Объяснение**:  
> Каждый символ — $i$ бит, всего $L$ символов → общее количество бит.

---

### 3. Количество байт на один номер

$$
\text{Байты} = \left\lceil \frac{B}{8} \right\rceil
$$

> 📌 **Объяснение**:  
> 1 байт = 8 бит.  
> Поскольку номер должен занимать **целое число байт**, округляем вверх.  
>  
> 🔹 Пример: $B = 10$ бит → $\lceil 10/8 \rceil = 2$ байта.

---

### 4. Общий объём памяти для $K$ номеров

$$
V = K \times \text{Байты}
$$

> 📌 **Объяснение**:  
> Каждый номер — $\text{Байты}$ байт, всего $K$ номеров → общий объём в байтах.

---

### 5. Условие задачи: «не менее $V_{\text{мин}}$»

$$
V \geq V_{\text{мин}}
$$

> 📌 **Объяснение**:  
> Объём памяти, необходимый для хранения всех номеров, должен быть **не меньше** заданного значения.  
>  
> 🔹 Пример: «не менее 7 Мбайт» → $V \geq 7 \times 1024 \times 1024$ байт.

---

## 🔄 Как использовать формулы в задаче

### Случай 1: Найти минимальную мощность алфавита $N$

Дано: $L$, $K$, $V_{\text{мин}}$  
Найти: минимальное целое $N$, такое что:  
$$
K \times \left\lceil \frac{L \times \lceil \log_2 N \rceil}{8} \right\rceil \geq V_{\text{мин}}
$$

> 💡 Решение: подбирать $N$ по возрастанию, пока неравенство не выполнится.

---

### Случай 2: Найти минимальную длину номера $L$

Дано: $N$, $K$, $V_{\text{мин}}$  
Найти: минимальное целое $L$, такое что:  
$$
K \times \left\lceil \frac{L \times \lceil \log_2 N \rceil}{8} \right\rceil \geq V_{\text{мин}}
$$

> 💡 Решение: подбирать $L$ по возрастанию, пока неравенство не выполнится.

---

### Случай 3: Найти минимальный объём памяти $V$

Дано: $N$, $L$, $K$  
Найти:  
$$
V = K \times \left\lceil \frac{L \times \lceil \log_2 N \rceil}{8} \right\rceil
$$

> 💡 Просто подставить и посчитать.

---

## 📏 Важные единицы измерения

| Единица       | В байтах           |
|---------------|--------------------|
| 1 бит         | 1/8 байта          |
| 1 байт        | 1 байт             |
| 1 Кбайт       | 1024 байта         |
| 1 Мбайт       | 1024 Кбайт = 1 048 576 байт |
| 1 Гбайт       | 1024 Мбайт         |

> ⚠️ В информатике: **1 Кбайт = 1024 байта**, а не 1000 — это критично для точных расчётов.

---
## 💡 Советы

- **Запишите формулы на листе** — они всегда одни и те же.
- **Следите за округлением** — $\lceil x \rceil$ — это наименьшее целое число, большее или равное $x$.
- **Не забывайте переводить Мбайт в байты** — $1 \text{ Мбайт} = 1024 \times 1024$ байт.
- **Если сомневаетесь — подставьте числа в формулу и посчитайте по шагам.**

# Примеры

---