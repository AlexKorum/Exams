---
aliases:
  - ege
  - exam
  - егэ
  - экзамен
tags:
  - ege
  - exam
date: 2026-01-13
---
# Описание

## Анализ скользящих окон в числовой последовательности 

В этом типе задач даётся **одна длинная последовательность чисел**, записанных **в столбик** (по одному числу в строке).  
Требуется **анализировать все возможные непрерывные подпоследовательности (окна)** фиксированной длины — например, по 3, 4, 5 или 6 чисел подряд — и **проверить, удовлетворяют ли они заданным условиям**.

Цель — **найти количество таких окон**, в которых выполняются **одно или несколько условий**, например:

- В окне **ровно одно число** делится на 3, остальные — нет. 
- **Сумма чисел в окне** кратна 7. 
- **Максимальное число в окне** больше **среднего арифметического** остальных. 
- **Произведение чисел в окне** чётно. 
- **Количество чётных чисел в окне** равно 2. 
- **Разность между максимальным и минимальным числом в окне** не превышает 10. 
- **Сумма чисел в окне** — нечётна. 
- **Все числа в окне различны**. 
- **Окно содержит ровно два одинаковых числа**, остальные — разные. 
- **Сумма квадратов чисел в окне** больше 1000. 

> ❗ Важно: условия часто **комбинируются** — нужно проверять, какие условия должны выполняться одновременно.

---

# Принцип решения

## 🧠 Общая логика подхода

Такие задачи требуют **анализа всех возможных непрерывных подпоследовательностей (окон)** фиксированной длины в длинной числовой последовательности.  
Каждое окно — это **подмассив из N последовательных чисел**, где N задано в условии (например, 3, 4, 5 и т.д.).  
Цель — **проверить для каждого окна, выполняются ли заданные условия**, и **подсчитать количество подходящих окон**.

Условия могут быть простыми (например, «сумма окна кратна 7») или сложными (например, «в окне ровно два одинаковых числа, остальные — разные»).

Решение строится на **переборе всех возможных окон** и **последовательной проверке условий** для каждого.

> 💡 Важно: условия часто **комбинируются** — нужно проверять, какие условия должны выполняться одновременно.

---

## 📋 Основные этапы решения

### 1. Прочитать данные из файла

- Открыть файл с последовательностью чисел.
- Прочитать все строки, преобразовать каждую в целое число.
- Сохранить в список — например, `numbers = [int(line.strip()) for line in f]`.

> 💻 Можно использовать Excel, но для автоматизации и точности лучше использовать Python.

---

### 2. Определить условия для каждого окна

- Выписать все условия из задачи.
- Перевести их на язык программирования.

> Примеры:

> - «в окне ровно одно число делится на 3» →  
>   `sum(1 for x in window if x % 3 == 0) == 1`

> - «все числа в окне различны» →  
>   `len(window) == len(set(window))`

> - «сумма чисел в окне кратна 7» →  
>   `sum(window) % 7 == 0`

> - «окно содержит ровно два одинаковых числа» →  
>   `sorted([window.count(x) for x in set(window)]) == [1, 1, 2]`

---

### 3. Перебрать все окна

- Для каждой позиции `i` от `0` до `len(numbers) - window_size`:
  - Выбрать окно: `window = numbers[i:i + window_size]`
  - Проверить все условия для этого окна.
  - Если все условия выполняются — увеличить счётчик.

> 🛠 Можно использовать функцию `check_window(window)` — где прописать условия из задачи.

---

### 4. Определить ответ

- Если нужно найти **количество окон** — вернуть счётчик.
- Если нужно найти **номер первого/последнего подходящего окна** — сохранить индекс `i`.
- Если нужно найти **максимальную/минимальную сумму окна** — сохранять и сравнивать значения.
- И т. д.

---

### 5. Записать ответ

- В ответе указать **целое число** — количество окон, номер окна, сумма и т.д.
- Проверить, что все условия учтены и не пропущены граничные случаи.

---

## 💡 Советы
- **Проверьте, как работает каждое условие** — например, `sum(1 for x in window if x % 3 == 0)` для разных окон.
- **Используйте `print` для отладки** — посмотрите, какие окна проходят проверку.
- **Если условие сложное — разбейте его на части** — например, сначала проверьте количество повторяющихся чисел, потом — сумму.
- **Учитывайте, что окна могут пересекаться** — это нормально, так и задумано.

---

# Шаблон кода

```python
def check_window(window: list) -> bool:
    # Пример условий — замените на условия из вашей задачи
    condition_1 = sum(1 for x in window if x % 3 == 0) == 1
    condition_2 = sum(window) % 7 == 0
    condition_3 = len(window) == len(set(window))
    return condition_1 and condition_2 and condition_3

def main():
    with open('data.txt') as f:
        numbers = [int(line.strip()) for line in f.readlines()]
    
    window_size = 4  # Замените на нужный размер окна
    count = 0
    
    for i in range(len(numbers) - window_size + 1):
        window = numbers[i:i + window_size]
        if check_window(window):
            count += 1
    
    print(count)

if __name__ == '__main__':
    main()
```
📝 Примечание:

- Замените `data.txt` на имя вашего файла.
- Замените `window_size` на требуемый размер окна (3, 4, 5 и т.д.).
- В функции `check_window` пропишите условия из вашей задачи.
- Если нужно найти не количество, а, например, номер окна — сохраняйте `i` и выводите его.