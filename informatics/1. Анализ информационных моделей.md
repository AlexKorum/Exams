---
aliases:
  - ege
  - exam
  - егэ
  - экзамен
tags:
  - ege
  - exam
date: 2025-12-19
---
# Описание
В этом типе заданий представлены два эквивалентных описания одной и той же сети — в виде **графа** и **таблицы**.

- **Граф** — визуальное представление, где вершины обозначены буквами (например, A, B, C…), а рёбра — линиями, соединяющими вершины. Иногда на рёбрах указаны веса (например, длины дорог в километрах).

- **Таблица** — числовое представление той же сети, где строки и столбцы соответствуют тем же вершинам, но обозначены цифрами (1, 2, 3…). В ячейках таблицы указаны веса рёбер между вершинами — или пусто, если связи нет.

📌 **Важно**: между буквами на графе и цифрами в таблице **нет заранее заданного соответствия** — оно должно быть установлено в ходе решения.

Цель — сопоставить буквы графа с цифрами таблицы, основываясь на структуре связей и значениях весов, а затем выполнить требуемое вычисление (например, найти сумму длин определённых рёбер или кратчайший путь).

## Виды задач
1. C указанием длин (весов) ребер [[Граф|графа]]
2. Без указания длин (весов) ребер [[Граф|графа]]

---
# Принцип решения
## Общая логика подхода

Такие задачи требуют установить соответствие между вершинами графа (обозначенными буквами) и строками/столбцами таблицы (обозначенными цифрами), когда это соответствие не задано явно. Известны лишь структура связей в графе и числовые значения весов рёбер в таблице.

Решение строится на **сопоставлении структурных свойств** — то есть на поиске такого соответствия букв и цифр, при котором все связи в графе находят точное отражение в таблице.

### Основные этапы решения:

1. **Выписать структуру графа**  
   Зафиксировать все пары вершин, соединённых рёбрами (например: AC, CB, BH и т.д.). Это задаёт «топологию» сети в буквенном виде.

2. **Анализировать таблицу**  
   Для каждой цифровой вершины (строки или столбца) определить, с какими другими вершинами она связана — то есть, какие номера присутствуют в соответствующей строке или столбце. Это задаёт «топологию» сети в числовом виде.

3. **Перебор гипотез соответствия**  
   Поскольку неизвестно, какая буква соответствует какому числу, перебираются все возможные варианты сопоставления букв цифрам. Это может быть сделано вручную (при малом числе вершин) или с помощью программирования (при большем числе).

4. **Проверка гипотезы**  
   Для каждой гипотезы (например: A=1, B=2, C=3…) проверяется, что каждое ребро графа (например, AC) имеет аналог в таблице — то есть, если A соответствует 1, а C — 3, то в таблице должна быть записана длина дороги между 1 и 3 (или наоборот, если таблица симметрична).

5. **Подтверждение и вычисление**  
   Как только найдено соответствие, при котором **все рёбра графа** находят подтверждение в таблице — гипотеза считается верной. Далее, используя установленное соответствие, выполняется требуемое вычисление (например, сумма длин двух конкретных рёбер).

6. **Запись ответа**  
   Ответ оформляется в соответствии с условием — как целое число, без дополнительных символов.

## Почему такой подход эффективен?

В условиях отсутствия явного соответствия между буквами и цифрами — **перебор всех возможных сопоставлений с последующей проверкой структуры связей** является надёжным методом. Он позволяет не просто угадать, а логически обосновать правильное соответствие. 

---
# Шаблон кода
```python
a = '368 34 126 27 67 135 458 17'.split()  
s = 'AC CB BH HD DA AG CG GE FE FH'.split()  
  
print(*range(1, 9))  
for p in permutations('ABCDEFGH'):  
    if all(str(p.index(x) + 1) in a[p.index(y)] for x, y in s):  
        print(*p)
```

---
# Примеры


---