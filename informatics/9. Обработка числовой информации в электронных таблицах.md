---
aliases:
  - ege
  - exam
  - егэ
  - экзамен
tags:
  - ege
  - exam
date: 2025-12-24
---
# Описание
## Анализ строк в электронной таблице по сложным условиям

В этом типе задач требуется **анализировать строки электронной таблицы**, каждая из которых содержит **фиксированное количество чисел** (например, 6, 7, 8 чисел).

Цель — **найти количество строк**, удовлетворяющих **одному или нескольким сложным условиям**, например:

- В строке есть число, которое повторяется **трижды**, остальные числа — **различны**.
- **Максимальное число** строки **не повторяется**.
- Сумма **минимального и максимального** из неповторяющихся чисел **не больше** суммы других неповторяющихся.
- В строке есть **ровно одно число**, которое повторяется **трижды**, и **одно число**, которое повторяется **дважды**.
- **Произведение трёх наибольших чисел** кратно **минимальному числу**.
- **Сумма всех повторяющихся чисел** больше **суммы всех неповторяющихся**.
- **Сумма повторяющихся чисел** равна **сумме неповторяющихся**.
- **Квадрат суммы повторяющихся чисел** больше **квадрата суммы неповторяющихся**.
- **Сумма всех чисел строки** — **нечётна**.

Цель — найти строки, удовлетворяющих всем условиям. Ответь на вопрос задачи используя полученные 

> ❗ Важно: условия часто **комбинируются** — нужно проверять какие  условия должны выполняться и в каких сочетаниях.

---
# Принцип решения
## 🧠 Общая логика подхода

Такие задачи требуют **анализа каждой строки таблицы** — обычно содержащей фиксированное количество чисел (например, 7 или 8 чисел) — и **проверки, удовлетворяет ли строка заданным условиям**. Условия могут быть простыми (например, «максимальное число не повторяется») или сложными (например, «в строке одно число повторяется трижды, остальные различны»).

Решение строится на **последовательной проверке условий** для каждой строки — с использованием стандартных операций: подсчёт повторений, поиск минимума/максимума, суммирование, сравнение и т.д.

> 💡 Важно: условия часто **комбинируются** — нужно проверять **как сочетаются условия**.

---

## 📋 Основные этапы решения

### 1. Прочитать данные из файла

- Открыть файл с таблицей.
- Преобразовать каждую строку в список чисел — например, с помощью `map(int, row.split())`.

> 💡 Можно использовать Excel, но проще использовать для сложных условий python

---

### 2. Определить условия для каждой строки

- Выписать все условия из задачи.
- Перевести их на язык программирования или логики.

> Пример:  
> - «в строке одно число повторяется трижды, остальные числа различны» →  
>   `sorted([row.count(x) for x in set(row)]) == [1, 1, 1, 1, 3]`  
> - «максимальное число строки не повторяется» →  
>   `row.count(max(row)) == 1`

---

### 3. Проверить каждую строку

- Для каждой строки — применить все условия.
- Если все условия выполняются — добавить строку в список результатов.

> 💡 Можно использовать функцию `check_conditions(row)` — где прописать условия из задачи.

---

### 4. Определить ответ

- Если нужно найти **количество строк** — вернуть `len(result)`.
- Если нужно найти **строку с максимальным/минимальным номером** — сохранить номер строки и выбрать нужный.
- Если нужно найти **минимальную/максимальную сумму чисел** — сохранить сумму и выбрать нужную.
- И т. д.

---

### 5. Записать ответ

- В ответе указать **целое число** — количество строк, номер строки, сумма чисел и т.д.
- Проверить, что все условия учтены.

---

## 💡 Советы

- **Не пытайтесь угадать** — перебор — это надёжный способ.
- **Проверьте, как работает каждое условие** — например, `row.count(x)` для каждого `x` в `set(row)`.
- **Используйте `print` для отладки** — посмотрите, какие строки проходят проверку.
- **Если условие сложное — разбейте его на части** — например, сначала проверьте, что есть число, повторяющееся трижды, потом — что остальные различны.
- **Учитывайте, что в строке может быть несколько одинаковых чисел** — не путайте «повторяющееся» и «неповторяющееся».

---
# Шаблон кода
```python
def check_conditions(row: list) -> bool:  
    condition_1 = sorted([row.count(x) for x in set(row)]) == [1, 1, 1, 1, 3]  
    condition_2 = row.count(max(row)) == 1  
    return condition_1 and condition_2  
  
  
def main():  
    with open('25348.txt') as f:  
        data = [list(map(int, row.split())) for row in f.readlines()]  
  
    result = []  
    for i, row in enumerate(data):  
        if check_conditions(row):  
            result.append((i, row))  
    print(len(result))  
  
  
if __name__ == '__main__':  
    main()
```

---
# Примеры

---